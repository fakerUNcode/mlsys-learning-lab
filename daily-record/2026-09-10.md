# 2026-09-10

## 今日主题

收缩实习前路线，并运行 Stage 1 的第一个 C++ Runtime 示例。

## 完成内容

- 将长期路线改为“实习前 9 阶段 + 实习后 4 条深化路线”；
- 明确实习前优先 CUDA、并行、算子优化和量化；
- 建立 Stage 1 的 C++ 学习目录、示例和报告；
- 使用 GNU C++ 13.3.0 完成 Debug 构建；
- 生成 `stage1_core`、`stage1_demo` 和 `stage1_test`；
- CTest 测试通过；
- 验证正常输入、非法输入与 shell 退出码；
- 启用 ASan 和 UBSan，构建与测试通过；
- 发现拆分示例后报告与 CMake 目标失配，确定源码、构建、测试、README 和报告必须整体维护。

## 今日证据

对应提交：

- `399241c docs: organize stage 1 C++ runtime learning path`；
- `fdbad79 docs: polish project and module readmes`；
- `8073bed docs: focus roadmap on pre-internship CUDA skills`；
- `2d143b7 docs: record stage 1 lifetime experiment`；
- `b8d73a1 refactor: split stage 1 lessons and examples`。

验证结果：

```text
stage1_test Passed
100% tests passed
0 tests failed out of 1
```

正常路径：

```text
count=4 sum=10
status=ok
```

错误路径返回 `status=invalid integer`，退出码为 `1`。

## 掌握边界

已运行 `vector`、`variant`、`optional` 和 `unique_ptr` 组成的示例，但当时尚未稳定区分左值引用、右值引用、所有者和借用者。真实 CUDA Buffer、Stream、动态库与线程问题尚未实验。

## 下一任务

恢复示例与报告的一致结构，再从最基础的类型、引用和生命周期开始学习，避免只会照抄运行命令。

## 直观理解

这一天既跑通了程序，也暴露了课程结构问题：能启动汽车不代表理解传动系统。后续需要把运行结果与每个对象的生命周期对应起来。
